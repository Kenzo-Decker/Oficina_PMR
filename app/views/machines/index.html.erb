<p id="notice"><%= notice %></p>

<%= render "layouts/navbar" %>

<div class='w-full bg-gray-200 py-20 flex flex-col justify-center items-center'>
  <div class='max-w-xl flex flex-col justify-center items-center'>
      <div>
          <p class='titulo'>Máquinas</p>
      </div>

      <table class='w-auto justify-between text-center border-2 border-yellow-500 bg-gray-100'>
            <col class='w-20'><col width=100><col width=100>
        
        <thead>
            <tr>
              <th>Nome</th>
              <th >Status</th>
              <th></th>
            </tr>
        </thead>
      
        <tbody>
            <% @machines.each do |machine| %>
                <tr>
                    <td><%= machine.name %></td>
                    <td><%= machine.state %></td>
                    <td><button class='btn'><%= link_to 'Editar', edit_machine_path(machine) %></button></td>
                </tr>
            <% end %>
        </tbody>
      </table>

    <div class='pt-5'>
        <button class='btn'>
            <a href="../">Voltar</a>
        </button>
    </div>
  </div>
</div>

<%= render "layouts/footer" %>


